#latest_blog_posts 
%h2 Latest Blog Posts
-@blog_posts.each do |blog_post|
  .blog_post
    =link_to(blog_post.title, blog_post_path(blog_post))
    / -blog_post.comments.each do |comment|
    /       =comment.message
    
%h2 Latest Comments
-@comments.each do |comment|
  .comment
    ="Geschrieben von: #{comment.name}"
    %br
    =link_to(comment.message, blog_post_path(comment.blog_post))
    %br
    %br